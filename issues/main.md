基于tauri实现一个邮箱客户端：
1.前端使用react框架，后端使用rust框架。
2.由rust来实现smtp/imap/pop3协议，可以使用现成的框架，提供api供前端调用。
3.前端使用react的api来实现ui，使用tauri的api来实现文件操作，数据库操作，用于存储账户信息，邮件信息等。
4.使用tauri的api来实现窗口操作，菜单操作，托盘操作等。
5.前端需要展现的功能包括：文件夹列表，邮件列表，邮件详情，账户设置，写信，收信，回复，转发，删除，移动，标记为已读，标记为未读，保存附件。
6.rust实现邮件的解析功能，可以使用现成的框架，提供api供前端调用。
